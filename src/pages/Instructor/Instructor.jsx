import { useEffect, useState } from "react";


const Instructor = () => {
    const [data, useData]= useState([])
    useEffect(()=>{
        fetch()
    }, [])
    return (
        <div>
            
        </div>
    );
};

export default Instructor;